id: bi-export-week-legacy
name: Company PM BI Export Week Legacy
version: 0.1.0
description: Legacy one-shot BI export chain for UnionTech AI weekly data
entry: run.sh
runtime: bash
os:
  - linux
requires:
  cli:
    - bash
    - python3
permissions:
  network: true
  workspace_only: true
notes:
  - This script is sourced from the local company-pm plugin repository
  - Prefer the v2 auth plus fetch split when possible
