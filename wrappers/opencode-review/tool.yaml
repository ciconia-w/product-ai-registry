id: opencode-review
name: OpenCode Review Wrapper
version: 0.1.0
description: Controlled local wrapper for repository review via opencode
entry: run.sh
runtime: bash
os:
  - linux
requires:
  cli:
    - opencode
  items:
    - addon:opencli
dispatch:
  runtime: opencode
  mode: wrapper
