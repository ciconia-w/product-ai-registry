agent_id: cursor
support_level: B
instruction_mode: agents_md_or_rules
skill_mode: generated_rules
script_mode: project_local_copy
wrapper_mode: project_local_copy
install_strategy: prompt_or_helper
supported_os:
  - linux
paths:
  rules_dir: ".cursor/rules"
  managed_root: "~/.ai-registry/materialized/cursor"
