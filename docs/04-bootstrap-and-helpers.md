# Bootstrap and Helpers

Prompt-first distribution remains the primary path for this registry.

Bootstrap helpers are not part of the maintained non-Linglong surface. If helper-based materialization returns, it should be reintroduced with fresh ownership, validation rules, and canonical documentation instead of relying on the removed skeleton entries.
